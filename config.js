/* =========================================================================
   КОНФИГУРАЦИЯ ПРИЛОЖЕНИЯ
   -------------------------------------------------------------------------
   Пароль НЕ хранится открытым текстом. Здесь лежит только SHA-256 хеш.
   Ввод пользователя хешируется и сравнивается с этим значением.

   Важно (честно): это статический сайт без бэкенда, поэтому хеш и логика
   проверки всё равно доступны в браузере. Хеширование убирает пароль из
   исходников открытым текстом, но не заменяет серверную авторизацию.
   Для настоящей защиты проверку пароля нужно выносить на сервер / в
   serverless-функцию (например, Netlify Functions) и держать секрет в env.

   Чтобы сменить пароль — посчитайте новый хеш:
     printf 'НОВЫЙ_ПАРОЛЬ' | sha256sum
   и вставьте сюда.
   Текущий хеш соответствует паролю: 012345678
   ========================================================================= */

window.APP_CONFIG = {
  // SHA-256 от строки пароля
  passwordHash: "36f50957f5e0b6ee3ef455674da35a86667f3314209dc1514c510fe95e840831",

  // Разрешённые к вводу цифры (0..8, без 9)
  allowedDigits: "012345678",

  // Диапазон авто-обновления данных, мс (5–10 секунд по ТЗ)
  refreshMinMs: 5000,
  refreshMaxMs: 10000,

  // Ключ хранилища настроек
  storageKey: "ratemon:v2",

  // Сколько точек истории держим на источник
  historyLimit: 600,
};
